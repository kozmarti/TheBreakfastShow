    {% for season in seasons %}
      <div>
          <a href="{{ path('season_show', {'id': season.id}) }}">Season {{season.number }}. :
            {{season.title }}
          </a>
      </div>
    {% endfor %}